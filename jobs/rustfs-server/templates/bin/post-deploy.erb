#!/bin/bash
set -eu

# Extension point for post-deploy automation.
# Operators may override this template to run bucket creation,
# policy application, or cluster validation steps after deployment.
echo "post-deploy: no actions configured"
exit 0
